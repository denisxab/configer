Сделать копию и скрыть данные
-----------------------------

Почти всегда нам нужно иметь в проекте секретные(приватные) данные, которые не должны стать публичными. У нас есть
возможность создавать копию конфигурации со скрытыми данными.

Для того чтобы указать переменную у которой нужно скрыть значения, напишите
в начел её имени ``_hide_`` данные префикс указан в  :meth:`logic_hideconf.hideconfLogic.hideconf`

нам нужно скрыть данные `url` для подключения к БД, ключ для шифрования сессии, данные для входа в админ панель,
ну или любые другие данные. Для того чтобы сделать копию этой конфигурации, например, для того чтобы сохранить всю
логики, но при этом скрыть выше указанные данные, мы указываем в начале имени `_hide_`, в итоге мы получим
файл `conf_pub.py` который можно спокойно хранить в открытом доступе, например в `GitHab`.

:configer/test/in/public/hideconf/conf.py:

.. literalinclude:: ../../configer/test/in/public/hideconf/conf.py


Выполним команду

.. code-block:: bash

    configer hideconf ./conf.py


В итоге мы получим копию, расположенную в `./conf_pub.py`. Содержание

:configer/test/out/public/hideconf/conf_pub.py:

.. literalinclude:: ../../configer/test/out/public/hideconf/conf_pub.py
